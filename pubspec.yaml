name: flexirank
description: A simple ranking system optimized for reordering lists.
version: 0.1.1
repository: https://github.com/goalfdn/flexirank-dart.git

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
